#include <stdio.h>

int sum(int a, int b){
    return a+b;
}

int main(){
    int num1, num2, ans;
    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    ans = sum(num1, num2);
    printf("Sum of the two numbers is: %d", ans);

    return 0;
}
